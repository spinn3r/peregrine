#!/bin/sh

branch=$(hg branch)

if [ "$branch" != "burton-cassandra-support" ]; then
    exit 125
fi

cp build.xml.working build.xml && ant clean jar compile.test
result=$?

#skip if the build failed.
if [ "$result" != "0" ]; then
    exit 125
fi

ant test
result=$?

exit $result
