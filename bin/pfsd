#!/bin/sh

export MAX_MEMORY=128M
export HOSTNAME=$(hostname)

# This option specifies the maximum total size of java.nio (New I/O package)
# direct buffer allocations.

export JAVA_OPTS="-XX:MaxDirectMemorySize=1024G"

ulimit -n 32768
ulimit -l unlimited


./bin/jexec peregrine.pfsd.Main $@