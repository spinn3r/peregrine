/**
 * Contains the configuration backend for peregrine and all associated classes;
 * The API needs to pass {@link peregrine.config.Config} objects within the
 * system and this is the main way of obtaining them.
 */
package peregrine.config;