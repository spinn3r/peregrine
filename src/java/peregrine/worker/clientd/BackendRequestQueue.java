package peregrine.worker.clientd;

/**
 * Handle registration of request to the backend into a queue which is then
 * serviced by a dedicated backend thread.
 */
public class BackendRequestQueue {

}
